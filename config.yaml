source: D:\Camara\a6400\
output: D:\Camara\OrganizacionSemantica
operation: copy

ollama:
  url: http://localhost:11434
  model: gemma4
  timeout_seconds: 120

description_prompt: |
  Genera una descripción semántica breve en español (1-2 frases) y luego
  una lista corta de etiquetas (5-8 palabras clave).

  Tu salida debe ser JSON con las claves "description" y "tags".

description:
  fallback_text: descripcion no disponible
  require_json: true

description_output: descriptions.jsonl

preprocessing:
  max_side: 1024
  jpeg_quality: 85
